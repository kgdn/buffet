services:
  server:
    build: server/
    devices:
      - "/dev/kvm:/dev/kvm"
    volumes:
      - iso:/app/iso
      - certs:/etc/ssl/certs
    ports:
      - 8000:8000
      - 5700:5700
  client:
    build: client/
    ports:
      - 443:443
    volumes:
      - certs:/etc/ssl/certs
